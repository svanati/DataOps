# -*- coding: utf-8 -*-
"""Confluent Cloud CLI MAnager

This module provides a simple way of managing a Confluent Cloud system via the
CLI API command(s).

Example:
        $ python py-ccloud-cli-worker

License:
        MIT

Attributes:
    title_row_index (int) This variable stores the index title row value.

    required_rows_length (int) This variable stores the index required rows
    value.

    env_login_email (string) This variable retrieves and stores the Confluent
    Cloud login email address value.

    env_login_password (string)
ENV_LOGIN_PASSWORD = "CCLOUD_PASSWORD"
    module_level_variable1 (int): Module level variables may be documented in
        either the ``Attributes`` section of the module docstring, or in an
        inline docstring immediately following the variable.

        Either form is acceptable, but the two should not be mixed. Choose
        one convention to document module level variables and be consistent
        with it.

Todo:
    * For module TODOs
    * You have to also use ``sphinx.ext.todo`` extension

.. _Google Python Style Guide:
   http://google.github.io/styleguide/pyguide.html

"""
